# minimal

Minimal Stratum bot using `MockBridge` — no Discord token required.

```bash
pnpm install
pnpm start
```

Useful for learning the command pipeline and for unit-style local runs. See [examples/README](../README.md).
